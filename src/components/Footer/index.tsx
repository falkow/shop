import React from 'react';

import styles from './Footer.module.scss';

const { wrapper } = styles;

const Footer = () => {
  return <footer className={wrapper}>Footer</footer>;
};

export default Footer;
